(function ($) {
    $(document).ready(function () {
        // Check if countdown elements exist
        const hasCountdownElements = $('#canvas-seconds').length && 
                                     $('#canvas-minutes').length && 
                                     $('#canvas-hours').length && 
                                     $('#canvas-days').length;

        // If elements are missing or countdown should be disabled
        if (!hasCountdownElements) {
            console.log("Countdown elements not found. Countdown disabled.");
            return;
        }

        // Log intentional disabling of the countdown
        console.log("Countdown intentionally disabled.");

        // Do not initialize the countdown
        // Comment out the countdown initialization logic like below:
        /*
        $("#countdown-container").final_countdown({
            start: Date.now(),
            end: Date.now() + 100000,
            now: Date.now()
        });
        */
    });
})(jQuery);
